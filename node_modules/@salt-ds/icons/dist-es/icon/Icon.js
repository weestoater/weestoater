import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { clsx } from 'clsx';
import { useWindow } from '@salt-ds/window';
import { useComponentCssInjection } from '@salt-ds/styles';
import css_248z from './Icon.css.js';

const makePrefixer = (prefix) => (...names) => [prefix, ...names].join("-");
const withBaseName = makePrefixer("saltIcon");
const DEFAULT_ICON_SIZE = 1;
const Icon = forwardRef(function Icon2({
  children,
  className,
  color = "inherit",
  size = DEFAULT_ICON_SIZE,
  style: styleProp,
  ...rest
}, ref) {
  const targetWindow = useWindow();
  useComponentCssInjection({
    testId: "salt-icon",
    css: css_248z,
    window: targetWindow
  });
  const style = {
    ...styleProp,
    "--saltIcon-size-multiplier": `${size}`
  };
  return /* @__PURE__ */ jsx("svg", {
    className: clsx(
      withBaseName(),
      { [withBaseName(color)]: color !== "inherit" },
      className
    ),
    style,
    role: "img",
    ...rest,
    ref,
    children: /* @__PURE__ */ jsx("g", {
      "aria-hidden": true,
      children
    })
  });
});

export { DEFAULT_ICON_SIZE, Icon, makePrefixer };
//# sourceMappingURL=Icon.js.map
