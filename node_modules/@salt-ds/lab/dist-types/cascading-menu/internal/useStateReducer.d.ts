import { flattenedMenuState } from "./stateUtils";
import { menuAction } from "./CascadingMenuAction";
export declare type stateItem = {
    id: string;
    highlightedItemIndex: number | null;
};
declare type state = stateItem[];
export declare function useStateReducer(menusDataById: flattenedMenuState | undefined, isNavigatingWithKeyboard: boolean): (state: state, action: menuAction) => state;
export {};
