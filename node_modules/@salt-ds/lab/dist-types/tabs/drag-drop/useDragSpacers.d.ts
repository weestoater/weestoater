import { MeasuredDropTarget } from "./drag-utils";
import { Direction } from "./dragDropTypes";
export declare const useDragSpacers: () => {
    displaceItem: (item: MeasuredDropTarget | null | undefined, size: number, useTransition?: boolean, direction?: Direction | undefined) => void;
    displaceLastItem: (item: MeasuredDropTarget, size: number, useTransition?: boolean) => void;
    clearSpacers: () => void;
};
