import { HTMLAttributes, ReactNode } from "react";
export declare type FormLabelNecessity = "optional" | "required";
export interface NecessityIndicatorOptions {
    /**
     * The FormField is required
     */
    required?: boolean;
    /**
     * Whether to show a label adornment for required or optional
     */
    displayedNecessity?: FormLabelNecessity;
    /**
     * Customize text displayed for necessity. Default to `(Required)` or `(Optional)`.
     */
    necessityText?: ReactNode;
    className?: string;
}
export declare const NecessityIndicator: ({ required, children, necessityText: necessityTextProp, displayedNecessity, className, ...restProps }: NecessityIndicatorOptions & HTMLAttributes<HTMLSpanElement>) => JSX.Element | null;
