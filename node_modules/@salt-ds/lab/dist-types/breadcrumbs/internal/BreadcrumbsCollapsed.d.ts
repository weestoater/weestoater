import { ReactNode } from "react";
import { CascadingMenuProps } from "../../cascading-menu";
import { MenuButtonProps } from "../../menu-button";
export interface BreadcrumbsCollapsedProps extends Omit<MenuButtonProps, "CascadingMenuProps"> {
    CascadingMenuProps?: CascadingMenuProps;
    accessibleText?: string;
    children: ReactNode;
    className?: string;
}
export declare const BreadcrumbsCollapsed: ({ children, CascadingMenuProps, accessibleText, ...rest }: BreadcrumbsCollapsedProps) => JSX.Element;
