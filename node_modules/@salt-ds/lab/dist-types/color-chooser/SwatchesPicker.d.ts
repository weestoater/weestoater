import { Color } from "./Color";
interface SwatchesPickerProps {
    allColors: string[][];
    color: Color | undefined;
    alpha?: number;
    onChange: (color: Color | undefined, finalSelection: boolean, e?: React.ChangeEvent) => void;
    onDialogClosed: () => void;
}
export declare const SwatchesPicker: ({ allColors, color, alpha, onChange, onDialogClosed, }: SwatchesPickerProps) => JSX.Element;
export {};
