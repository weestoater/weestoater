import { ChangeEvent } from "react";
import { Color } from "./Color";
interface SwatchProps {
    active: boolean;
    alpha: number;
    color: string;
    onClick: (color: Color | undefined, finalSelection: boolean, e?: ChangeEvent) => void;
    onDialogClosed: () => void;
    transparent?: boolean;
}
export declare const Swatch: ({ color, onClick, active, alpha, onDialogClosed, transparent, }: SwatchProps) => JSX.Element;
export {};
