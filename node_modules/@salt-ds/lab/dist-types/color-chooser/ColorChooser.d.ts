import { ChangeEvent } from "react";
import { ButtonProps } from "@salt-ds/core";
import { Color } from "./Color";
export interface ColorChooserProps {
    color: Color | undefined;
    defaultAlpha?: number;
    disableAlphaChooser?: boolean;
    displayHexOnly?: boolean;
    hideLabel?: boolean;
    onClear: () => void;
    onSelect: (color: Color | undefined, finalSelection: boolean, event?: ChangeEvent) => void;
    placeholder?: string;
    buttonProps?: Partial<ButtonProps>;
    saltColorOverrides?: Record<string, string>;
    showSwatches?: boolean;
    showColorPicker?: boolean;
    readOnly?: boolean;
}
export declare const ColorChooser: ({ onClear, onSelect, color, showSwatches, showColorPicker, defaultAlpha, disableAlphaChooser, hideLabel, placeholder, buttonProps, saltColorOverrides, readOnly, displayHexOnly, }: ColorChooserProps) => JSX.Element;
