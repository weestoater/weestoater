import { Color } from "./Color";
export interface SwatchesTabProps {
    allColors: string[][];
    color: Color | undefined;
    alpha: number;
    handleColorChange: (color: Color | undefined, finalSelection: boolean, e?: React.ChangeEvent) => void;
    displayColorName: string | undefined;
    placeholder: string | undefined;
    onDialogClosed: () => void;
}
export declare const Swatches: ({ allColors, color, alpha, handleColorChange, displayColorName, placeholder, onDialogClosed, }: SwatchesTabProps) => JSX.Element;
