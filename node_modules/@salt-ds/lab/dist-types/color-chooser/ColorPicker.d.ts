import { ChangeEvent } from "react";
import { Color } from "./Color";
export interface ColorPickerProps {
    disableAlphaChooser: boolean;
    color?: Color;
    alpha?: number;
    onChange: (color: Color | undefined, finalSelection: boolean, e?: ChangeEvent) => void;
    onDialogClosed: () => void;
}
export declare const ColorPicker: ({ alpha, disableAlphaChooser, color, onChange, onDialogClosed, }: ColorPickerProps) => JSX.Element;
