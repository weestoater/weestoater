import { HTMLAttributes, MouseEventHandler, ReactNode } from "react";
export interface TreeNodeProps extends Omit<HTMLAttributes<HTMLLIElement>, "onMouseEnter"> {
    children?: ReactNode;
    description?: string;
    highlighted?: boolean;
    idx?: number;
    isLeaf?: boolean;
    label?: string;
    onMouseEnter?: MouseEventHandler;
    selected?: boolean;
}
export declare const TreeNode: ({ "aria-level": ariaLevel, children, className: classNameProp, description, highlighted, idx, isLeaf, label, onMouseEnter, selected, ...props }: TreeNodeProps) => JSX.Element;
