'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var React = require('react');
var core = require('@salt-ds/core');
var useResizeObserver = require('./useResizeObserver.js');

const NONE = [];
function useWidth(responsive) {
  const [width, setWidth] = React.useState();
  const ref = React.useRef(null);
  const handleResize = React.useCallback(({ width: newWidth }) => {
    setWidth(newWidth);
  }, []);
  const measurementsToObserve = responsive ? useResizeObserver.WidthOnly : NONE;
  useResizeObserver.useResizeObserver(ref, measurementsToObserve, handleResize);
  core.useIsomorphicLayoutEffect(() => {
    if (!ref.current) {
      return void 0;
    }
    handleResize(ref.current.getBoundingClientRect());
  }, [handleResize, responsive]);
  return [ref, width];
}

exports.useWidth = useWidth;
//# sourceMappingURL=useWidth.js.map
