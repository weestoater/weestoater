'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var React = require('react');
var core = require('@salt-ds/core');
var DescendantContext = require('./DescendantContext.js');

function useDescendant(descendant) {
  const { assigning, setItems } = React.useContext(DescendantContext.DescendantContext);
  const index = React.useRef(-1);
  core.useIsomorphicLayoutEffect(() => {
    if ((assigning == null ? void 0 : assigning.current) && setItems) {
      setItems((old) => {
        index.current = old.length;
        return old.concat(descendant);
      });
    }
  });
  return index.current;
}

exports.useDescendant = useDescendant;
//# sourceMappingURL=useDescendant.js.map
