'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

function getAnnouncement(itemCount = 0, firstItem) {
  return itemCount === 0 ? "no results" : `${itemCount} ${itemCount > 1 ? "results" : "result"}${firstItem ? `, ${firstItem}` : ""}`;
}

exports.getAnnouncement = getAnnouncement;
//# sourceMappingURL=getAnnouncement.js.map
