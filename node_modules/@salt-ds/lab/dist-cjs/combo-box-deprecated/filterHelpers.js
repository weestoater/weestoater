'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var escapeRegExp = require('../utils/escapeRegExp.js');
require('react');
require('@salt-ds/core');

const leftTrim = (value) => value ? value.replace(/^\s+/g, "") : value;
const getDefaultFilterRegex = (value) => new RegExp(`(${escapeRegExp.escapeRegExp(leftTrim(value))})`, "gi");
const getDefaultFilter = (inputValue = "", getFilterRegex = getDefaultFilterRegex) => (itemValue = "") => Boolean(itemValue.length) && Boolean(inputValue.length) && itemValue.match(getFilterRegex(inputValue)) !== null;

exports.getDefaultFilter = getDefaultFilter;
exports.getDefaultFilterRegex = getDefaultFilterRegex;
//# sourceMappingURL=filterHelpers.js.map
