'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var ColorPicker = require('./ColorPicker.js');
var Swatches = require('./Swatches.js');

const createTabsMapping = ({
  swatches,
  colorPicker,
  disableAlphaChooser,
  allColors,
  color,
  alpha,
  handleColorChange,
  displayColorName,
  placeholder,
  onDialogClosed
}) => {
  if (!swatches && !colorPicker) {
    throw new Error("You cannot select no color chooser tabs");
  }
  return {
    ...swatches && {
      Swatches: {
        Component: Swatches.Swatches,
        props: {
          allColors,
          color,
          alpha,
          handleColorChange,
          displayColorName,
          placeholder,
          onDialogClosed
        }
      }
    },
    ...colorPicker && {
      "Color Picker": {
        Component: ColorPicker.ColorPicker,
        props: {
          disableAlphaChooser,
          color,
          alpha,
          onChange: handleColorChange,
          onDialogClosed
        }
      }
    }
  };
};

exports.createTabsMapping = createTabsMapping;
//# sourceMappingURL=createTabsMapping.js.map
