import { ColorPicker } from './ColorPicker.js';
import { Swatches } from './Swatches.js';

const createTabsMapping = ({
  swatches,
  colorPicker,
  disableAlphaChooser,
  allColors,
  color,
  alpha,
  handleColorChange,
  displayColorName,
  placeholder,
  onDialogClosed
}) => {
  if (!swatches && !colorPicker) {
    throw new Error("You cannot select no color chooser tabs");
  }
  return {
    ...swatches && {
      Swatches: {
        Component: Swatches,
        props: {
          allColors,
          color,
          alpha,
          handleColorChange,
          displayColorName,
          placeholder,
          onDialogClosed
        }
      }
    },
    ...colorPicker && {
      "Color Picker": {
        Component: ColorPicker,
        props: {
          disableAlphaChooser,
          color,
          alpha,
          onChange: handleColorChange,
          onDialogClosed
        }
      }
    }
  };
};

export { createTabsMapping };
//# sourceMappingURL=createTabsMapping.js.map
