import { useState } from 'react';

function usePopperStatus(props) {
  const { initialOpen, autoClose } = props;
  const [isOpen, setIsOpen] = useState(!!initialOpen);
  const onKeyDown = (event) => {
    if (event.altKey && ["ArrowUp", "ArrowDown"].includes(event.key)) {
      setIsOpen((x) => !x);
    } else if (!isOpen && ["Alt", "Tab", "Escape", "Control", "Shift", "Meta"].includes(event.key)) {
      setIsOpen(true);
    } else if (isOpen && event.key === "Escape") {
      setIsOpen(false);
    } else if (isOpen && autoClose && event.key === "Enter") {
      setIsOpen(false);
    }
  };
  const onFocus = (event) => {
    setIsOpen(true);
  };
  const onBlur = (event) => {
    setIsOpen(false);
  };
  const onChange = () => {
    if (autoClose) {
      setIsOpen(false);
    }
  };
  const onClick = () => {
    if (!isOpen) {
      setIsOpen(true);
    }
  };
  return {
    isOpen,
    onKeyDown,
    onFocus,
    onBlur,
    onChange,
    onClick
  };
}

export { usePopperStatus };
//# sourceMappingURL=usePopperStatus.js.map
