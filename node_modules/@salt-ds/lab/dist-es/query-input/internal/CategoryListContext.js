import { createContext, useContext } from 'react';

const CategoryListContext = createContext(void 0);
function useCategoryListContext() {
  const context = useContext(CategoryListContext);
  if (!context) {
    throw new Error(
      `useCategoryListContext should be used inside CategoryList`
    );
  }
  return context;
}

export { CategoryListContext, useCategoryListContext };
//# sourceMappingURL=CategoryListContext.js.map
