import { jsx } from 'react/jsx-runtime';
import { makePrefixer } from '@salt-ds/core';
import { clsx } from 'clsx';
import css_248z from './OverflowSeparator.css.js';
import { useWindow } from '@salt-ds/window';
import { useComponentCssInjection } from '@salt-ds/styles';

const withBaseName = makePrefixer("saltOverflowSeparator");
const OverflowSeparator = (props) => {
  const { className, focusable, ...rest } = props;
  const targetWindow = useWindow();
  useComponentCssInjection({
    testId: "salt-overflow-separator",
    css: css_248z,
    window: targetWindow
  });
  return /* @__PURE__ */ jsx("div", {
    className: clsx(withBaseName(), className),
    ...rest
  });
};
OverflowSeparator.defaultProps = {
  focusable: false
};

export { OverflowSeparator };
//# sourceMappingURL=OverflowSeparator.js.map
