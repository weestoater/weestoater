import { jsx } from 'react/jsx-runtime';
import { clsx } from 'clsx';
import { forwardRef } from 'react';
import { makePrefixer } from '@salt-ds/core';
import css_248z from './StaticInputAdornment.css.js';
import { useWindow } from '@salt-ds/window';
import { useComponentCssInjection } from '@salt-ds/styles';

const withBaseName = makePrefixer("saltStaticInputAdornment");
const StaticInputAdornment = forwardRef(function StaticInputAdornment2(props, ref) {
  const { children, className, ...other } = props;
  const targetWindow = useWindow();
  useComponentCssInjection({
    testId: "salt-static-input-adornments",
    css: css_248z,
    window: targetWindow
  });
  return /* @__PURE__ */ jsx("div", {
    className: clsx(withBaseName(), className),
    ref,
    ...other,
    children
  });
});

export { StaticInputAdornment };
//# sourceMappingURL=StaticInputAdornment.js.map
