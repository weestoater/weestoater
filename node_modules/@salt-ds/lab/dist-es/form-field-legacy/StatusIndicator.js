import { jsx } from 'react/jsx-runtime';
import { clsx } from 'clsx';
import { makePrefixer, StatusIndicator as StatusIndicator$1, Tooltip } from '@salt-ds/core';

const withBaseName = makePrefixer("saltFormField");
const StatusIndicator = (props) => {
  const {
    TooltipComponent = Tooltip,
    hasTooltip = false,
    TooltipProps: TooltipProps2,
    tooltipText,
    IconProps: IconProps2,
    className,
    status = "info",
    ...restProps
  } = props;
  return /* @__PURE__ */ jsx(TooltipComponent, {
    hideIcon: true,
    status,
    content: tooltipText,
    placement: "top",
    disabled: !hasTooltip,
    ...TooltipProps2,
    children: /* @__PURE__ */ jsx(StatusIndicator$1, {
      status,
      ...restProps,
      ...IconProps2,
      className: clsx(
        withBaseName("statusIndicator"),
        className,
        IconProps2 == null ? void 0 : IconProps2.className
      )
    })
  });
};

export { StatusIndicator };
//# sourceMappingURL=StatusIndicator.js.map
