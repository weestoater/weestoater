import { useRef, useEffect } from 'react';

function useMountedRef() {
  const mountedRef = useRef(false);
  useEffect(() => {
    mountedRef.current = true;
    return () => {
      mountedRef.current = false;
    };
  }, []);
  return mountedRef;
}

export { useMountedRef };
//# sourceMappingURL=useMountedRef.js.map
