import { useState } from 'react';

const useSlideSelection = (initialValue) => {
  const [selectedSlide, setSelectedSlide] = useState(initialValue || 0);
  const [selectedTransition, setSelectedTransition] = useState(void 0);
  const handleSlideSelection = (sliderIndex, transition) => {
    const newTransition = transition ? transition : selectedSlide < sliderIndex ? "increase" : "decrease";
    setSelectedSlide(sliderIndex);
    setSelectedTransition(newTransition);
  };
  return [selectedTransition, selectedSlide, handleSlideSelection];
};

export { useSlideSelection };
//# sourceMappingURL=useSlideSelection.js.map
