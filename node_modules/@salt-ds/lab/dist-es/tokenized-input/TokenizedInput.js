import { jsx } from 'react/jsx-runtime';
import { useForkRef } from '@salt-ds/core';
import { forwardRef } from 'react';
import { TokenizedInputBase } from './TokenizedInputBase.js';
import { useTokenizedInput } from './useTokenizedInput.js';

const TokenizedInput = forwardRef(function TokenizedInput2(props, ref) {
  const { inputRef: inputRefProp, ...restProps } = props;
  const { inputRef, helpers, inputProps } = useTokenizedInput(restProps);
  return /* @__PURE__ */ jsx(TokenizedInputBase, {
    helpers,
    inputRef: useForkRef(inputRef, inputRefProp),
    ref,
    ...inputProps
  });
});

export { TokenizedInput };
//# sourceMappingURL=TokenizedInput.js.map
