import { jsx } from 'react/jsx-runtime';
import { forwardRef, createContext, useContext } from 'react';

const Window = forwardRef(function Window2({ children, open, ...props }, ref) {
  return /* @__PURE__ */ jsx("div", {
    ...props,
    ref,
    children
  });
});
const WindowContext = createContext(Window);
const useWindow = () => useContext(WindowContext);
const globalObject = typeof global === "undefined" ? window : global;
const isDesktop = globalObject.isDesktop;

export { Window, WindowContext, isDesktop, useWindow };
//# sourceMappingURL=WindowContext.js.map
