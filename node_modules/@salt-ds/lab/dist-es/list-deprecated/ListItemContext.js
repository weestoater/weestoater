import { createContext, useContext } from 'react';

const ListItemContext = createContext(void 0);
const useListItemContext = function() {
  const contextValue = useContext(ListItemContext);
  if (contextValue === void 0) {
    throw new Error(
      "useListItemContext must be used inside of a List or ListBase component."
    );
  }
  return contextValue;
};

export { ListItemContext, useListItemContext };
//# sourceMappingURL=ListItemContext.js.map
