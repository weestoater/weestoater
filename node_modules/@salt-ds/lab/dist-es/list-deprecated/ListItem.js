import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import { isPlainObject } from './internal/helpers.js';
import { useDescendant } from './internal/DescendantContext.js';
import { useListItem } from './useListItem.js';
import { ListItemBase } from './ListItemBase.js';

function ListItem(props, ref) {
  const {
    children,
    item = props.item === void 0 && !isPlainObject(children) ? children : props.item,
    ...restProps
  } = props;
  const { itemToString, itemProps } = useListItem({
    index: useDescendant(item),
    item,
    ...restProps
  });
  const itemText = itemToString(item);
  return /* @__PURE__ */ jsx(ListItemBase, {
    tooltipText: itemText,
    ...itemProps,
    ref,
    children: children !== void 0 ? children : itemText
  });
}
const _ListItem = forwardRef(ListItem);

export { _ListItem as ListItem };
//# sourceMappingURL=ListItem.js.map
