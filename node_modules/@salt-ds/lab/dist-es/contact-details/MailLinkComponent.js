import { jsx } from 'react/jsx-runtime';
import { Link, Text } from '@salt-ds/core';
import { isEmail } from '../utils/isEmail.js';
import 'react';

const MailLinkComponent = (props) => {
  const { value, ...restProps } = props;
  return isEmail(value) ? /* @__PURE__ */ jsx(Link, {
    href: `mailto:${value}`,
    tabIndex: 0,
    maxRows: 1,
    ...restProps,
    children: value
  }) : /* @__PURE__ */ jsx(Text, {
    maxRows: 1,
    children: value
  });
};

export { MailLinkComponent };
//# sourceMappingURL=MailLinkComponent.js.map
