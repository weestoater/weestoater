import { useContext } from 'react';
import { useIsomorphicLayoutEffect } from '@salt-ds/core';
import { FormFieldLegacyContext } from './FormFieldLegacyContext.js';

function useFormFieldLegacyProps({
  focusVisible
} = {}) {
  const { setFocused, ...formFieldProps } = useContext(FormFieldLegacyContext) || {};
  useIsomorphicLayoutEffect(() => {
    if (focusVisible !== void 0 && setFocused) {
      setFocused(focusVisible);
    }
  }, [focusVisible, setFocused]);
  return {
    setFocused,
    ...formFieldProps
  };
}

export { useFormFieldLegacyProps };
//# sourceMappingURL=useFormFieldLegacyProps.js.map
